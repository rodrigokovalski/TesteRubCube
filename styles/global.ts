import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    body: {
        padding: 16,
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'wrap'
    },
    busca: {
        borderColor: '#a3a3a3',
        borderWidth: 1,
        margin: 16,
        borderRadius: 10,
        paddingVertical: 8,
        paddingHorizontal: 12
    }
});