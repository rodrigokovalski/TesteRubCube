import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    view: {
        flexDirection: 'column',
        width: '33%',
        borderRadius: 12,
        padding: 12,
        marginBottom: 8,
    },
    image: {
        width: 100,
        height: 100,
        borderRadius: 8,
    },
    title: {
        textAlign: 'center',
        fontSize: 12,
        fontWeight: 700,
        marginTop: 12,
    },
});